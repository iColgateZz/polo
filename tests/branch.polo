void testBranch(num x) {
    if (x < 0) {
        print "negative";
    } elif (x == 0) {
        print "zero";
    } else {
        print "positive";
    }
}

num main() {
    testBranch(-3);  // "negative"
    testBranch(0);   // "zero"
    testBranch(9);   // "positive"
    return 0;
}
