num a = 5;
num b = 3;

num func(num a, num a);

num func(num a, num b) {
    num c;
    num d;
}

num x = func(a, b) + 15;

// comment

// todo
// check parameters
// check arguments
// check their types
// when calling a function, check argnum and types
// check return type of a function
// check redefinition of local variables
// take identifiers in function decl into account